# FamilyOS 3.4.0 Remediation Backlog

## Release 3.4.1 disposition

- AUD-340-001: Resolved. Reconciliation targets Release 3.4 and the aggregate gate is restored.
- AUD-340-002: Resolved. Stale presentation preconditions were removed while downstream workflows remain fully asserted.
- AUD-340-003: Resolved. Dynamic two-household owner/adult/teen-member-equivalent/child authorization is enforced against real test APIs and Storage.
- AUD-340-004: Partially resolved. The blank chain passes on a disposable PostgreSQL cluster and the hosted test project was upgraded from its observed 3.3 state; a separately recreated hosted blank project is not required by the permanent local gate.
- AUD-340-005: Resolved by attribution and deferred remediation. The legacy call is in Vercel CLI 56.2.0 tooling; no FamilyOS call site or user impact was found.
- AUD-340-006: Resolved. `test:db-3.4` is part of `check`.

## Must fix before next feature release

1. **AUD-340-001 / AUD-340-006: Restore a truthful green release gate**
   - Update `scripts/validate-supabase-bootstrap.mjs` from Release 3.3 to 3.4 counts, version, migration, and output.
   - Add `pnpm run test:db-3.4` to `pnpm run check`.
   - Acceptance: `pnpm run check` returns nonzero on any child failure and passes end to end on clean 3.4.0.

2. **AUD-340-002: Repair the full Playwright regression suite**
   - Replace presentation-copy preconditions with stable roles or test IDs.
   - Navigate directly to Relationships and Family Assistant before module assertions.
   - Confirm whether removed Home sections were intentional product behavior.
   - Acceptance: 81/81 existing tests pass across desktop, 390 px mobile, tablet, and dark mode.

3. **AUD-340-003: Prove Release 3.4 authorization and lifecycle boundaries**
   - Build a disposable two-household identity matrix for owner, adult, member, and child/read-only roles.
   - Exercise direct REST SELECT/INSERT/UPDATE/DELETE against all four 3.4 tables and recommendation history.
   - Acceptance: same-household allowed operations and cross-household/unauthorized operations are explicitly asserted.

## Recommended for 3.4.1

1. Add a Release 3.4 Playwright/API workflow for View, Complete, Snooze, Dismiss, Never Remind, Reviewed, feedback, reset, and export.
2. Prove confirmation gates for Create Task, Route to Calendar, and Route to Maintenance, including no authoritative mutation before acceptance.
3. Run blank-chain and Release 3.3-to-3.4 upgrade validation in a disposable hosted or local database with pre/post row counts.
4. Add settings-persistence, household-scoped reset/export, timezone/day-boundary, material-change resurfacing, and repeated-dismissal suppression cases.
5. Re-run the complete gate and archive command-level evidence in the 3.4.1 validation record.

## Can defer

1. Broad Create React App or dependency modernization solely for deprecation warnings.
2. Cosmetic copy refinements unrelated to the three stale test expectations.
3. Exhaustive wide-desktop and device catalog testing after the representative release matrix is green.
4. Advanced performance tuning without measured production latency or request-volume evidence.

## Test automation improvements

1. Add one dedicated `tests/e2e/release-3.4.spec.js` instead of relying on source-pattern validation.
2. Separate Home presentation tests from owning-module workflows so a copy change does not block deep functional coverage.
3. Add stable semantic selectors for Family Brief sections and recommendation lifecycle actions.
4. Add dynamic RLS tests using two households and at least four supported roles.
5. Add Calendar authenticated CRUD/failure tests, recommendation routing tests, and settings reset/export tests.
6. Add session-expiration, offline recovery, slow response, double-click, refresh-during-mutation, direct URL, back-button, DST, and midnight cases for critical workflows.
7. Add a test-report summary that preserves pass/fail/skip counts and command exit codes without PowerShell wrapper masking.

## Documentation and operational improvements

1. Document the exact safe workflow for returning the dedicated test project to a blank state before initialization validation.
2. Document the PostgreSQL client discovery path or make the scripts resolve the installed client reliably.
3. Record which validation scripts are static, local disposable, hosted test, or production read-only.
4. Capture the next `/api/weather` or `/api/calendar` DEP0169 stack trace and record runtime/package ownership.
5. Add a lightweight production observability checklist for 4xx/5xx, latency, duplicate requests, and API call volume without exposing household data.
