const WEATHER_CODES={0:"Clear",1:"Mostly clear",2:"Partly cloudy",3:"Cloudy",45:"Fog",48:"Fog",51:"Light drizzle",53:"Drizzle",55:"Heavy drizzle",61:"Light rain",63:"Rain",65:"Heavy rain",71:"Light snow",73:"Snow",75:"Heavy snow",80:"Rain showers",81:"Rain showers",82:"Heavy showers",95:"Thunderstorms",96:"Thunderstorms",99:"Severe thunderstorms"};
export default async function handler(req,res){res.setHeader("Cache-Control","s-maxage=1800, stale-while-revalidate=3600");const location=String(req.query?.location||"").trim();if(!location)return res.status(400).json({error:"Location required"});try{const geo=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(location)}&count=1&language=en&format=json`);const place=(await geo.json()).results?.[0];if(!place)return res.status(404).json({error:"Location unavailable"});const result=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${place.latitude}&longitude=${place.longitude}&current=temperature_2m,weather_code&daily=temperature_2m_max,temperature_2m_min,precipitation_probability_max,weather_code&temperature_unit=fahrenheit&timezone=auto&forecast_days=1`);const data=await result.json();const precipitation=Number(data.daily?.precipitation_probability_max?.[0]||0);const code=data.current?.weather_code;return res.status(200).json({temperature:Math.round(data.current?.temperature_2m),condition:WEATHER_CODES[code]||"Current conditions",high:Math.round(data.daily?.temperature_2m_max?.[0]),low:Math.round(data.daily?.temperature_2m_min?.[0]),note:precipitation>=40?`${precipitation}% chance of precipitation`:code>=95?"Severe weather possible":""});}catch{return res.status(503).json({error:"Weather unavailable"});}}
