import { deduplicateRecommendations, recommendationKey } from "./recommendationDedupe";
test("deduplicates the same issue across surfaces",()=>{const items=[{title:"Pool chlorine low",sourceModule:"pool",relatedRecordIds:["1"]},{title:"Different wording",sourceModule:"pool",relatedRecordIds:["1"]}];expect(deduplicateRecommendations(items)).toHaveLength(1);expect(recommendationKey(items[0])).toBe("pool:1");});
test("suppresses dismissed feedback until the reminder interval",()=>{const item={title:"Call Alex",sourceModule:"relationships",relatedRecordIds:["person-1"]},key=recommendationKey(item);expect(deduplicateRecommendations([item],[{recommendation_key:key,status:"dismissed",updated_at:"2026-07-14T00:00:00Z"}],{now:"2026-07-15T00:00:00Z"})).toEqual([]);expect(deduplicateRecommendations([item],[{recommendation_key:key,status:"dismissed",updated_at:"2026-07-01T00:00:00Z"}],{now:"2026-07-15T00:00:00Z"})).toHaveLength(1);});
