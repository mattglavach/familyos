import { ACTION_TYPES, deterministicAdvisory, validateAdvisoryResponse } from "./advisoryContract";

test("validates the structured advisory contract",()=>{const result=validateAdvisoryResponse({summary:"Review today",recommendations:[{id:"r1",title:"Plan task",priority:"high",sourceModule:"tasks",evidenceLevel:"confirmed",proposedAction:{type:"create_task",payload:{title:"Prepare"}}}],sourceModules:["tasks"]});expect(result.ok).toBe(true);expect(result.value.recommendations[0].approvalRequired).toBe(true);});
test("removes malformed or unsupported proposed actions",()=>{const result=validateAdvisoryResponse({summary:"Safe summary",recommendations:[{id:"r1",title:"Unsafe",proposedAction:{type:"delete_everything",payload:{}}}]});expect(result.ok).toBe(false);expect(result.value.recommendations[0].proposedAction).toBeUndefined();});
test("supports every approved action type and no autonomous delete action",()=>{expect(ACTION_TYPES).toContain("create_task");expect(ACTION_TYPES).toContain("add_pool_test_recommendation");expect(ACTION_TYPES.some(type=>/^delete_/.test(type))).toBe(false);});
test("builds a deterministic provider-outage fallback",()=>{const result=deterministicAdvisory({modules:{tasks:{importantItems:[{title:"Pay bill",dueDate:"2026-07-14",priority:"high"}]}}});expect(result.fallback).toBe(true);expect(result.recommendations[0]).toEqual(expect.objectContaining({title:"Pay bill",evidenceLevel:"confirmed"}));});
